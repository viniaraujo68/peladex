[pytest]
testpaths = tests
addopts = -ra
markers =
    timing: wall-clock sensitive (argon2 timing equalization); deselect with -m "not timing"
    ratelimit: mutates global limiter state; must not run in parallel
